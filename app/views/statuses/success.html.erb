<%= render 'layouts/text_banner', locals: { title: "Success" } %>

<div class="page-content">
    <div class="wrap">
      <% flash.each do |key, value| %>
        <p><%= sanitize(value) %></p>
      <% end %>   
    </div><!--.wrap-->
</div><!--.page-content-->