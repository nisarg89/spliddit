Hello <%= @agent.name %>,

You've been invited to use Spliddit's Distributing Tasks App for <%= @agent.instance.name %>. The below link will ask you to evaluate all of the tasks. No registration is required.

<%= link_to assigning_tasks_instance_url(@agent.instance, p: @agent.passcode), assigning_tasks_instance_url(@agent.instance, p: @agent.passcode), only_path: false %>

<%= render 'spliddit_info', :locals => {:application => "tasks"} %>