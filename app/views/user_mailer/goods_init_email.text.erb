Hello <%= @agent.name %>,

You've been invited to use Spliddit's Dividing Goods App for <%= @agent.instance.name %>. The below link will ask you to evaluate all of the items. No registration is required.

<%= link_to dividing_goods_instance_url(@agent.instance, p: @agent.passcode), dividing_goods_instance_url(@agent.instance, p: @agent.passcode), only_path: false %>

<%= render 'spliddit_info', :locals => {:application => "goods"} %>